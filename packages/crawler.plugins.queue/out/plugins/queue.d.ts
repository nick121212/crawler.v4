export declare class QueuePlugin {
    /**
     * 分析urls
     * @param param0
     */
    private getUrls({queueItem, discoverConfig, queueConfig});
    /**
     * 地址规范化
     * @param param0
     */
    private queueUrl({urls, queueItem, queueConfig});
}
