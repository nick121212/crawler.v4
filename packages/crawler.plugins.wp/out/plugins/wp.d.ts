export declare class WpPlugin {
    private wpApi;
    private blog(config, options);
    private getCategory(api, category, parent?);
    private getUser(api, data);
    private qa(config, options);
    private user(config, options);
    private ct(config, options);
    private youlai(config, options);
    private init(msg, options, globalOptions);
}
