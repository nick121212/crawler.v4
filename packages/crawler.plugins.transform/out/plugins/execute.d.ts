export declare class TransformExexutePlugin {
    /**
     * 启动一个任务
     * @param param0 数据
     */
    private single({expression, data});
    /**
     * 启动一个任务
     * @param param0 数据
     */
    private muti({expressions, data}, options?, globalOptions?);
}
